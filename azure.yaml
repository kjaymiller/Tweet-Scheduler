# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: flask-boilerplate-aca
metadata:
  template: ""
services:
  web:
    project: ./src
    module: containerapp/
    language: py
    host: containerapp